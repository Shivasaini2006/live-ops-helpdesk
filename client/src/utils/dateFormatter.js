/**
 * @file dateFormatter.js
 * @description Date formatting utilities.
 * @responsibility Provides functions to format raw ISO dates into human-readable strings (e.g. "July 15, 2026 2:40 PM", "5 mins ago").
 */

/**
 * Formats a Date object or ISO date string into a standard format.
 * @param {Date|string} date - Date to format.
 * @returns {string} Formatted output string.
 */
export const formatDate = (date) => {
  // TODO: Format using options or date-fns
  return '';
};

/**
 * Returns a relative time string (e.g., "3 minutes ago").
 * @param {Date|string} date - Date to format.
 * @returns {string} Relative time string.
 */
export const formatRelativeTime = (date) => {
  // TODO: Calculate delta and return relative representation
  return '';
};
